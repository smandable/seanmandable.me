---
title: Hello, world
date: 2026-04-19
description: The first post on the new site.
draft: false
---

This is a placeholder post so the Writing section has something to render. Add new markdown files alongside this one in `src/content/writing/` and they'll show up automatically.

## Frontmatter

Every post needs:

- `title` — string
- `date` — ISO date (`2026-04-19`)
- `description` — optional, shown on the index
- `draft` — optional, defaults to `false`; `true` hides the post from the build
